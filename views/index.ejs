<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <style>
    :root {
      --primary: #2c3e50;
      --danger: #e74c3c;
      --light: #ecf0f1;
    }
    
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      background-color: #f9f9f9;
    }
    
    header {
      background: var(--primary);
      color: white;
      padding: 20px;
      border-radius: 8px 8px 0 0;
      text-align: center;
    }
    
    .access-alert {
      background: var(--danger);
      color: white;
      padding: 15px;
      text-align: center;
      font-weight: bold;
      border-radius: 5px;
      margin: 20px 0;
      box-shadow: 0 3px 10px rgba(231, 76, 60, 0.3);
    }
    
    .user-card {
      background: white;
      border-radius: 8px;
      padding: 20px;
      box-shadow: 0 2px 15px rgba(0,0,0,0.1);
      text-align: center;
    }
    
    .user-avatar {
      width: 100px;
      height: 100px;
      border-radius: 50%;
      background: var(--primary);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      margin: 0 auto 15px;
    }
    
    .endpoints {
      margin-top: 30px;
      background: white;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 15px rgba(0,0,0,0.1);
    }
    
    .endpoint {
      padding: 10px;
      border-bottom: 1px solid #eee;
      font-family: monospace;
    }
  </style>
</head>
<body>
  <header>
    <h1><%= title %></h1>
    <p>Backend Status: <strong style="color:#2ecc71">Operational</strong></p>
  </header>

  <div class="access-alert">
    <%= accessMessage %>
  </div>

  <div class="user-card">
    <div class="user-avatar">
      <%= userName.charAt(0).toUpperCase() %>
    </div>
    <h2>Welcome, <%= userName %></h2>
    <p>You're viewing a restricted server information page</p>
  </div>

  <div class="endpoints">
    <h2>Available Endpoints</h2>
    <div class="endpoint">
      GET /user/<span style="color:#e74c3c">:username</span> → Returns JSON user data
    </div>
    <div class="endpoint">
      GET / → Server information (current page)
    </div>
  </div>
</body>
</html>